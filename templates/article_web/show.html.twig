{% extends 'base.html.twig' %}

{% block title %}ArticleWeb{% endblock %}

{% block body %}
    <h1>ArticleWeb</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_web.id }}</td>
            </tr>
            <tr>
                <th>NomArticle</th>
                <td>{{ article_web.nomArticle }}</td>
            </tr>
            <tr>
                <th>Theme</th>
                <td>{{ article_web.theme }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('article_web_index') }}">back to list</a>

    <a href="{{ path('article_web_edit', {'id': article_web.id}) }}">edit</a>

    {{ include('article_web/_delete_form.html.twig') }}
{% endblock %}
